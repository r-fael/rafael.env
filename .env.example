# Prismic repository name.
# Optional — overrides the repository targeted by the Prismic client.
# When unset, falls back to "repositoryName" in slicemachine.config.json.
NEXT_PUBLIC_PRISMIC_ENVIRONMENT=your-repo-name
